"use strict";

/**            BOUCLE
 *              FOR
 * Declarer une variable a qui est egale à un entier positif
 * 
 * Faire un console log de 'OKLM' autant de fois que le valeur de a
 * 
 * 
 */

let a2 = 5;                         // la variable est bien egal a un entier positif
for (let i = 0 ; i < a2 ; i++){     // donc il faut que la base soit de 0 et ++ pour que ca reste positif
    console.log("OKLM");
}